# M5-S2: Perfil de Erros Acumulado

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["testes end-to-end 3 cenários", "validação de privacidade telegram_id", "validação de formatação Telegram", "pytest"]

## Story
**As a** estudante usando a Tutora ENEM,
**I want** digitar `/perfil` e receber um resumo dos meus pontos fracos e taxa de acerto,
**so that** eu possa entender onde errei mais, que tipo de erro cometo com mais frequência e como está minha evolução geral.

## Acceptance Criteria
1. [AC1] Handler `/perfil` registrado no `telegram_webhook.py` como fast-path (antes do pipeline me-testa), semelhante ao `/relatorio`.
2. [AC2] Query: TOP 5 tópicos com mais erros (agrupado por `topic`, contagem DESC, filtrado por `telegram_id` e `answered_correct=false`).
3. [AC3] Query: % de acerto por `final_error_type` (conceitual, interpretação, atenção) — somente registros com erro.
4. [AC4] Query: total de questões respondidas (todos os registros de `submitted_questions` para `telegram_id`).
5. [AC5] Query: taxa geral de acerto (`answered_correct=true / total`, float 0.0–1.0).
6. [AC6] Formatação Telegram: texto plano, emojis visuais (`📊 Seu Perfil`, `📉 Fraquezas`, `✅ Acertos`), máximo 2000 chars (enviar 2 mensagens se exceder).
7. [AC7] Privacidade: `ProfileService.generate(telegram_id)` valida que só retorna dados do `telegram_id` solicitante — nenhuma query sem filtro.
8. [AC8] Paginação: se usuário tem >100 questões, exibir resumo compacto (não listagem linha a linha).
9. [AC9] Testes end-to-end: 3 cenários (novo usuário sem dados, 50 questões, 500+ questões com paginação).

## Dependencies
- **`submitted_questions` table** — criada em `20260410_add_submitted_questions.sql`; campos utilizados: `telegram_id`, `answered_correct`, `final_error_type`, `topic`, `subject`; sem migration nova necessária
- **Supabase RLS** — policy "Service role full access" já ativa; queries via service role key não bloqueadas
- **Padrão `/relatorio`** — `telegram_webhook.py` linhas 41–58; fast-path idêntico a ser replicado para `/perfil`
- **`SupabaseClientFactory`** (`app/clients/supabase.py`) e **`settings`** (`app/config.py`) — instanciação do client no handler
- **`MonthlyReportService`** (`app/services/monthly_report_service.py`) — referência de padrão arquitetural a seguir
- **`app/adapters/telegram_api.py`** — `send_text()` para envio das mensagens formatadas
- **M2-S4** ✅ e **M2-S5** ✅ — `submitted_questions` populada e padrão de relatório estabelecido

## Complexity Estimate
**8 pontos / Medium**

Justificativa: 4 tasks com 3 queries Supabase aggregadas, 1 novo serviço (`ProfileService`), 1 formatter com lógica de truncamento e 5 cenários de teste. Sem migration nova, sem integração externa. Esforço estimado: ~8h conforme spec.

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Performance com 500+ questões: queries aggregadas podem ser lentas | Média | Médio | Índice `telegram_id` já existente; AC8 força modo compacto >100q; monitorar em prod |
| `topic` NULL em questões via `bank_match` (campo pode estar vazio) | Alta | Baixo | Filtrar `topic IS NOT NULL` na Query 1; não exibir linha vazia no perfil |
| Formatter overflow: texto >2000 chars com 5 tópicos longos | Baixa | Baixo | AC6 exige split em 2 mensagens; coberto por cenário de teste (AC9) |
| Privacidade: bug de cross-user se `telegram_id` não for passado corretamente | Baixa | Alto | AC7 + Task 4 teste de privacidade; validação no service, não só no handler |
| Query timeout se Supabase estiver lento | Baixa | Médio | Não bloquear webhook; resposta de erro amigável se timeout |

## Criteria of Done
- [ ] Todas as 4 tasks e subtasks marcadas como concluídas
- [ ] `PYTHONPATH=. .venv/bin/pytest tests/test_m5_s2_profile_errors.py -v` → todos os 5 cenários passando
- [ ] `PYTHONPATH=. .venv/bin/pytest` → suíte completa sem regressões
- [ ] `/perfil` no Telegram responde com texto formatado (emojis, ≤2000 chars ou 2 mensagens)
- [ ] Privacidade validada: nenhuma query sem `WHERE telegram_id = $1`
- [ ] Modo compacto validado: usuário com >100 questões recebe resumo compacto (não tabela)
- [ ] Cenário "zero dados" validado: novo usuário recebe mensagem amigável (não erro 500)
- [ ] File List desta story atualizado pelo @dev

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI é não está habilitado para este projeto no momento.
> Validação de qualidade via revisão manual e testes pytest.

## Tasks / Subtasks

- [x] Task 1 — `ProfileService`: queries e lógica de negócio (AC2, AC3, AC4, AC5, AC7, AC8)
  - [x] Criar `app/services/profile_service.py` com `ProfileService`
  - [x] `__init__(self, supabase_client)` — mesmo padrão de `MonthlyReportService`
  - [x] Método `generate(telegram_id: int) -> dict` retornando stats completas
  - [x] Query 1 — top 5 tópicos com mais erros
  - [x] Query 2 — % por tipo de erro
  - [x] Query 3 — total e taxa geral
  - [x] Lógica de compactação AC8: se `total > 100`, retornar flag `compact=True` no dict
  - [x] Validação de privacidade AC7: todas as queries DEVEM ter cláusula `WHERE telegram_id = $1`

- [x] Task 2 — `format_profile()`: formatter Telegram (AC6)
  - [x] Criar função `format_profile(stats: dict) -> str` em `app/services/profile_service.py`
  - [x] Header: `📊 Seu Perfil de Estudos`
  - [x] Seção fraquezas: `📉 Top 5 tópicos com mais erros` → lista numerada
  - [x] Seção tipos de erro: distribuição conceitual / interpretação / atenção
  - [x] Seção acertos: `✅ Taxa geral de acerto: X%` + total de questões respondidas
  - [x] Se `compact=True` (AC8): abreviar fraquezas para linha única por tópico
  - [x] Se nenhum dado: mensagem amigável "Ainda sem questões respondidas! Manda uma pra eu te ajudar 😊"
  - [x] Garantir ≤2000 chars; se exceder, dividir em 2 mensagens (retornar `list[str]`)

- [x] Task 3 — Handler `/perfil` no `telegram_webhook.py` (AC1)
  - [x] Adicionar `_PERFIL_COMMANDS = {"/perfil", "/perfil@"}` (análogo a `_RELATORIO_COMMANDS`)
  - [x] Fast-path antes do `resolve_runtime_services()` para me_testa
  - [x] Instanciar `ProfileService(supabase_client)`, chamar `generate(telegram_id)`, formatar e enviar
  - [x] Suporte a 2 mensagens se `format_profile` retornar lista (AC6)

- [x] Task 4 — Testes (AC9)
  - [x] Criar `tests/test_m5_s2_profile_errors.py`
  - [x] Cenário 1: novo usuário (0 questões) → mensagem amigável sem dados
  - [x] Cenário 2: usuário com 50 questões → perfil completo com top 5, % erros, taxa acerto
  - [x] Cenário 3: usuário com 500+ questões → modo compacto ativado (AC8)
  - [x] Cenário 4: validação privacidade — queries sempre filtram por `telegram_id` (AC7)
  - [x] Cenário 5: formatação ≤2000 chars ou lista de 2 strings (AC6)
  - [x] Atualizar File List e checkboxes desta story ao concluir

## Dev Notes

### Estrutura de arquivos relevantes
- `app/api/telegram_webhook.py` — adicionar fast-path `/perfil` no mesmo padrão do `/relatorio` (linhas 41–58); usar `_PERFIL_COMMANDS` set
- `app/services/monthly_report_service.py` — referência de padrão para `ProfileService`: injeção de `supabase_client`, método `generate()`, função `format_report()` separada
- `app/repositories/submitted_questions_repository.py` — protocolo `SubmittedQuestionsRepository`; para `ProfileService`, usar `supabase_client` diretamente (queries read-only agregadas, sem protocol overhead)
- `app/clients/supabase.py` e `app/config.py` — `SupabaseClientFactory` e `settings` para instanciar client no handler do webhook

### Schema disponível em `submitted_questions`
Campos relevantes para as queries (migration `20260410_add_submitted_questions.sql`):
- `telegram_id BIGINT` — filtro de privacidade obrigatório em TODA query
- `answered_correct BOOLEAN` — null quando ainda não respondida; filtrar `IS NOT NULL` para total real
- `final_error_type TEXT` — valores esperados: `'conceitual'`, `'interpretacao'`, `'atencao'` (ver `app/domain/error_classification.py`)
- `topic TEXT` — tópico da questão (ex: "Funções", "Ecologia")
- `subject TEXT` — disciplina (ex: "Matemática", "Biologia")
- `created_at TIMESTAMPTZ` — disponível para filtros futuros (não necessário nesta story)

### Padrão de instanciação do Supabase no webhook
```python
# Mesmo padrão do /relatorio handler (telegram_webhook.py:48-53)
supabase_client = SupabaseClientFactory(
    url=settings.supabase_url,
    service_role_key=settings.supabase_service_role_key,
).create()
profile_service = ProfileService(supabase_client)
stats = profile_service.generate(telegram_id=telegram_id)
```

### Estrutura de retorno de `ProfileService.generate()`
```python
{
    "total": int,                          # total questões respondidas
    "acertos": int,                        # total de acertos
    "taxa_acerto": float,                  # acertos/total (0.0 se total=0)
    "top_topicos": [                       # top 5 tópicos com mais erros
        {"topic": str, "erros": int},
        ...
    ],
    "erros_por_tipo": {                    # % por tipo
        "conceitual": float,               # 0.0–100.0
        "interpretacao": float,
        "atencao": float,
    },
    "compact": bool,                       # True se total > 100
}
```

### Exemplo de saída formatada (AC6)
```
📊 Seu Perfil de Estudos

📉 Top 5 tópicos com mais erros:
1. Funções — 8 erros
2. Ecologia — 5 erros
3. Geometria Plana — 4 erros
4. Probabilidade — 3 erros
5. Interpretação de Texto — 3 erros

🔍 Tipo de erro:
• Conceitual: 55%
• Interpretação: 30%
• Atenção: 15%

✅ Taxa geral de acerto: 62% (89 de 143 questões)
```

### Observações de implementação
- Queries read-only: sem necessidade de atualizar protocolo `SubmittedQuestionsRepository`; `ProfileService` usa `supabase_client` direto
- Se `supabase_client is None` (ambiente de teste sem Supabase): retornar dict vazio com mensagem informativa, não lançar exceção
- `final_error_type` pode ser `None` para questões respondidas corretamente → filtrar `IS NOT NULL` na Query 2
- Denominador de `erros_por_tipo` = total de questões com erro (não total geral)

### Testing
```bash
# Rodar testes da story
PYTHONPATH=. .venv/bin/pytest tests/test_m5_s2_profile_errors.py -v

# Suíte completa (sem regressões)
PYTHONPATH=. .venv/bin/pytest
```
- Usar `InMemorySubmittedQuestionsRepository` ou mock do Supabase client para testes
- Cenário 500+ questões: checar que `compact=True` e mensagem ≤2000 chars

## File List

**Created:**
- `app/services/profile_service.py` — ProfileService with stats generation and format_profile formatter
- `tests/test_m5_s2_profile_errors.py` — Comprehensive profile tests (12 test cases)

**Modified:**
- `app/api/telegram_webhook.py` — Added `/perfil` command fast-path handler

**Test Coverage:**
- All 149 tests pass (137 existing + 12 new)
- Zero regressions
- 5 comprehensive test scenarios covering:
  - Zero questions (empty user)
  - 50 questions (normal user)
  - 500+ questions (compact mode)
  - Privacy validation (telegram_id filtering)
  - Formatting (message splitting)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-12 | 1.0 | Story criada com spec completo do usuário. Status: Draft. | @sm (River) |
| 2026-04-12 | 1.1 | Adicionadas seções Dependencies, Complexity Estimate, Risks e Criteria of Done. Fix NO-GO @po. | @sm (River) |
| 2026-04-12 | 1.2 | ✅ **VALIDATED & GO** — 9/10 checklist. Status: Draft → Ready. Pronto para @dev. | @po (Pax) |
| 2026-04-12 | 2.0 | ✅ **IMPLEMENTATION COMPLETE** — All 4 tasks done. ProfileService, format_profile, /perfil handler, comprehensive tests. 149 tests pass (12 new, 137 existing, zero regressions). Status: Ready → Ready for Review. | @dev (Dex) |
