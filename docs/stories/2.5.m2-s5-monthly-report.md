# M2-S5: Relatório Mensal de Progresso

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["testes end-to-end", "validação de formatação Telegram", "validação de filtro por período"]

## Story
**As a** estudante usando a Tutora ENEM,
**I want** receber um relatório mensal com meu progresso de estudos via Telegram,
**so that** eu possa acompanhar minha evolução, identificar pontos fracos e manter a motivação ao ver o quanto já avancei.

## Acceptance Criteria
1. O relatório é gerado via comando manual (`/relatorio`) e/ou job mensal agendado, exibindo dados dos últimos 30 dias.
2. O relatório inclui obrigatoriamente: total de questões respondidas, taxa de acerto (%), top 3 tópicos com mais erros, total de flashcards Anki gerados e progresso geral vs meta configurada.
3. A formatação é otimizada para Telegram (sem markdown quebrado, emojis visuais como separadores, linhas curtas), exibida como mensagem de texto sem arquivo externo.
4. O filtro de período aceita os últimos 30 dias por padrão e aceita parâmetro customizado (ex: `--dias 15`).
5. O relatório utiliza as tabelas existentes: `submitted_questions` (questões enviadas), `study_sessions` (histórico), `alternatives.explanation` (comentários), campos `subject`/`topic` (classificação) e `apkg_generated` (status Anki).
6. Testes end-to-end cobrem: geração com dados reais, período vazio (zero questões), e formatação do texto de saída.
7. O job mensal é implementado como script Python executável de forma autônoma (ex: `scripts/monthly_report.py`), sem depender de estado de sessão ativa.

## Tasks / Subtasks
- [x] Criar `app/services/monthly_report_service.py` com queries para agregar dados dos últimos N dias.
- [x] Implementar query de total de questões e taxa de acerto a partir de `study_sessions` e `submitted_questions`.
- [x] Implementar query de top 3 tópicos com mais erros (agrupamento por `subject`/`topic`).
- [x] Implementar contagem de flashcards gerados via campo `apkg_generated`.
- [x] Criar função de formatação do relatório para Telegram (texto plano com emojis, sem markdown complexo).
- [x] Adicionar handler `/relatorio` no `app/api/telegram_webhook.py` para disparo manual.
- [x] Criar script `scripts/monthly_report.py` para execução autônoma (job agendado / cron).
- [x] Criar `tests/test_m2_s5_monthly_report.py` com testes end-to-end cobrindo os 3 cenários do AC-6.
- [x] Atualizar File List e checkboxes desta story ao concluir.

## Dev Notes
- Dados disponíveis: `submitted_questions` (enunciado, gabarito, `answered_correct`, `retry_attempts`, `sent_to_anki`, `apkg_generated`), `study_sessions` (histórico de sessões), `alternatives.explanation`, campos `subject` e `topic`.
- A tabela `submitted_questions` foi criada na migration `supabase/migrations/20260410_add_submitted_questions.sql` (M2-S4).
- O adapter Telegram já existe em `app/adapters/telegram_api.py` — reutilizar para envio.
- Referência de formatação: mensagens da Aria no `me_testa_answer_service.py` — mesmo tom acolhedor.
- O script `scripts/monthly_report.py` deve funcionar standalone (sem servidor FastAPI rodando), conectando diretamente ao Supabase via variáveis de ambiente.
- Meta de progresso: usar valor fixo configurável (ex: variável de ambiente `MONTHLY_GOAL_QUESTIONS=100`) para o MVP; não requer tabela de metas neste momento.

## Testing
```bash
# Rodar todos os testes da story
python -m pytest tests/test_m2_s5_monthly_report.py -v

# Smoke do script standalone
python scripts/monthly_report.py --telegram-id <ID_TESTE> --dias 30

# Rodar suite completo (sem regressão)
python -m pytest
```

## File List
- `docs/stories/2.5.m2-s5-monthly-report.md`
- `app/services/monthly_report_service.py` (novo)
- `app/api/telegram_webhook.py` (handler `/relatorio` adicionado)
- `scripts/monthly_report.py` (novo)
- `tests/test_m2_s5_monthly_report.py` (novo — 22 testes)

## Change Log
- 2026-04-10: Story criada por @po (exceção pragmática — conteúdo completamente especificado pelo usuário). Status: Draft. Aguardando validação formal antes de passar para Ready.
- 2026-04-10: @dev YOLO — implementação completa. 126 testes passando (22 novos). Status: Ready for Review.
